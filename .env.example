# =============================================================================
# ATS Resume Builder - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values
# NEVER commit the .env file to version control!
# =============================================================================

# OpenRouter API Key (Required)
# Get your free API key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-your-api-key-here

# Server Port (Optional, defaults to 3001)
PORT=3001

# Allowed Origins for CORS (Optional)
# Comma-separated list of frontend URLs that can access this API
# Defaults to: http://localhost:3000,http://localhost:5500,http://127.0.0.1:5500
ALLOWED_ORIGINS=https://my-ats-resume.vercel.app,http://localhost:3000,http://localhost:5500,http://127.0.0.1:5500

# =============================================================================
# SECURITY NOTES:
# =============================================================================
# 1. Never expose your API key in the frontend code
# 2. Always use environment variables for sensitive data
# 3. In production, set ALLOWED_ORIGINS to your actual domain
# 4. Consider adding authentication (JWT) for multi-user scenarios
# =============================================================================
